import 'package:flutter/material.dart';

class ElementosScreen extends StatelessWidget {
   
const ElementosScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
         child: Text('ElementosScreen'),
      ),
    );
  }
}