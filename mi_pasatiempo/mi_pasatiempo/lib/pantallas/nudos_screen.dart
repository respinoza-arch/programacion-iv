import 'package:flutter/material.dart';

class NudosScreen extends StatelessWidget {
   
const NudosScreen({super.key});
  
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
         child: Text('NudosScreen'),
      ),
    );
  }
}